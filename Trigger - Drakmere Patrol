Trigger Name: Util: Drakmere Patrol
pattern: (You find no such living creature.|are fighting each other|That requires an explanation mark|You cannot attack (.) as (.) is in your team)
javascript
regexp

*****Change last line to your special attack or delete. Either way*****


// Get the userdata
    let index = gwc.userdata.drakmere.index
    const paths = gwc.userdata.drakmere.path 
    const step = paths[index]

    // Turn off the trigger when finished
    if (index > paths.length - 1) {
    gwc.trigger.disable('Util: Drakmere Patrol')
    gwc.trigger.disable('Util: Drakmere Patrol kill next')
    return;
    }

    // Move to next room
    gwc.connection.send(step)

    // Increase index and follow through with next action in room.

    gwc.userdata.drakmere.index = ++index;
    gwc.connection.send("kill human")
    gwc.connection.send("slash")
